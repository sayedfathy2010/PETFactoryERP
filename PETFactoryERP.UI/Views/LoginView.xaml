<Window x:Class="PETFactoryERP.UI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تسجيل الدخول" Height="300" Width="450" FlowDirection="RightToLeft" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Text="انتاج PET - نظام إدارة" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
        <StackPanel Grid.Row="1" VerticalAlignment="Top">
            <Label Content="اسم المستخدم"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="300"/>
            <Label Content="كلمة المرور" Margin="0,10,0,0"/>
            <PasswordBox x:Name="pwdBox" Width="300" PasswordChanged="PwdBox_PasswordChanged"/>
            <Button Content="تسجيل الدخول" Width="150" Margin="0,20,0,0" HorizontalAlignment="Center" Command="{Binding LoginCommand}"/>
        </StackPanel>
    </Grid>
</Window>
